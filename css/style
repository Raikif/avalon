/* ========== Base Styles ========== */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

:root {
    --color-good: #3498db;
    --color-evil: #e74c3c;
    --color-gold: #f39c12;
    --color-dark: #1a1a2e;
    --color-darker: #16213e;
    --color-light: #eee;
    --color-success: #27ae60;
    --color-fail: #c0392b;
}

body {
    font-family: 'Roboto', sans-serif;
    background: linear-gradient(135deg, var(--color-dark), var(--color-darker));
    color: var(--color-light);
    min-height: 100vh;
}

h1, h2, h3 {
    font-family: 'Cinzel', serif;
}

.hidden {
    display: none !important;
}

/* ========== Buttons ========== */
.btn {
    padding: 12px 24px;
    border: none;
    border-radius: 8px;
    font-size: 16px;
    font-weight: bold;
    cursor: pointer;
    transition: all 0.3s ease;
    text-transform: uppercase;
}

.btn:disabled {
    opacity: 0.5;
    cursor: not-allowed;
}

.btn-primary {
    background: linear-gradient(135deg, var(--color-gold), #e67e22);
    color: var(--color-dark);
}

.btn-primary:hover:not(:disabled) {
    transform: translateY(-2px);
    box-shadow: 0 4px 15px rgba(243, 156, 18, 0.4);
}

.btn-secondary {
    background: transparent;
    border: 2px solid var(--color-light);
    color: var(--color-light);
}

.btn-large {
    padding: 16px 32px;
    font-size: 18px;
}

.btn-approve {
    background: var(--color-good);
    color: white;
    flex: 1;
    padding: 20px;
    font-size: 18px;
}

.btn-reject {
    background: var(--color-evil);
    color: white;
    flex: 1;
    padding: 20px;
    font-size: 18px;
}

.btn-success {
    background: var(--color-success);
    color: white;
    width: 100%;
    padding: 20px;
    font-size: 18px;
}

.btn-fail {
    background: var(--color-fail);
    color: white;
    width: 100%;
    padding: 20px;
    font-size: 18px;
    margin-top: 10px;
}

/* ========== Landing Page ========== */
.landing-page {
    display: flex;
    justify-content: center;
    align-items: center;
}

.landing-page .container {
    text-align: center;
    padding: 40px;
}

.logo h1 {
    font-size: 4rem;
    color: var(--color-gold);
    text-shadow: 0 0 20px rgba(243, 156, 18, 0.5);
    margin-bottom: 10px;
}

.subtitle {
    font-size: 1.5rem;
    opacity: 0.8;
    margin-bottom: 60px;
}

.menu-options {
    display: flex;
    flex-direction: column;
    gap: 20px;
    max-width: 300px;
    margin: 0 auto;
}

/* Modal */
.modal {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0,0,0,0.8);
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 1000;
}

.modal-content {
    background: var(--color-darker);
    padding: 30px;
    border-radius: 16px;
    text-align: center;
    max-width: 400px;
    width: 90%;
}

.modal-content h2 {
    margin-bottom: 20px;
    color: var(--color-gold);
}

.modal-content input {
    width: 100%;
    padding: 15px;
    margin-bottom: 15px;
    border: 2px solid #333;
    border-radius: 8px;
    background: var(--color-dark);
    color: white;
    font-size: 16px;
    text-align: center;
}

.modal-buttons {
    display: flex;
    gap: 10px;
}

.modal-buttons .btn {
    flex: 1;
}

/* ========== Host Page ========== */
.host-container {
    min-height: 100vh;
    padding: 20px;
}

.game-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px;
    background: rgba(0,0,0,0.3);
    border-radius: 12px;
    margin-bottom: 20px;
}

.game-header h1 {
    color: var(--color-gold);
    font-size: 2rem;
}

.game-code-display {
    font-size: 1.5rem;
    font-family: 'Courier New', monospace;
    background: var(--color-dark);
    padding: 10px 20px;
    border-radius: 8px;
}

.game-code-display span {
    color: var(--color-gold);
    font-weight: bold;
}

/* Lobby */
.lobby-layout {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: 40px;
    margin-bottom: 30px;
}

.qr-section {
    text-align: center;
    background: rgba(255,255,255,0.1);
    padding: 30px;
    border-radius: 16px;
}

.qr-section h2 {
    margin-bottom: 20px;
    color: var(--color-gold);
}

#qrCode {
    background: white;
    padding: 20px;
    border-radius: 12px;
    display: inline-block;
    margin-bottom: 15px;
}

#qrCode canvas {
    display: block;
}

.join-url {
    font-family: 'Courier New', monospace;
    font-size: 0.9rem;
    opacity: 0.7;
    word-break: break-all;
}

.players-section h2 {
    margin-bottom: 20px;
    color: var(--color-gold);
}

.players-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
    gap: 15px;
}

.player-card {
    background: rgba(255,255,255,0.1);
    padding: 20px;
    border-radius: 12px;
    text-align: center;
    animation: fadeIn 0.3s ease;
}

@keyframes fadeIn {
    from { opacity: 0; transform: scale(0.8); }
    to { opacity: 1; transform: scale(1); }
}

.player-card .avatar {
    width: 60px;
    height: 60px;
    background: var(--color-gold);
    border-radius: 50%;
    margin: 0 auto 10px;
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: 24px;
    color: var(--color-dark);
}

.role-settings {
    background: rgba(255,255,255,0.05);
    padding: 20px;
    border-radius: 12px;
    margin-bottom: 20px;
}

.role-settings h3 {
    margin-bottom: 15px;
    color: var(--color-gold);
}

.role-toggles {
    display: flex;
    flex-wrap: wrap;
    gap: 20px;
}

.role-toggles label {
    display: flex;
    align-items: center;
    gap: 8px;
    cursor: pointer;
}

.role-toggles input[type="checkbox"] {
    width: 20px;
    height: 20px;
    cursor: pointer;
}

#startGameBtn {
    display: block;
    margin: 0 auto;
}

/* Game Screen */
#gameScreen {
    padding: 20px;
}

.mission-track {
    text-align: center;
    margin-bottom: 20px;
}

.mission-track h3 {
    color: var(--color-gold);
    margin-bottom: 15px;
}

.mission-tokens {
    display: flex;
    justify-content: center;
    gap: 20px;
}

.mission-token {
    width: 100px;
    height: 100px;
    border-radius: 50%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    font-weight: bold;
    background: rgba(255,255,255,0.1);
    border: 3px solid #555;
    transition: all 0.3s ease;
}

.mission-token.success {
    background: var(--color-good);
    border-color: var(--color-good);
}

.mission-token.fail {
    background: var(--color-evil);
    border-color: var(--color-evil);
}

.mission-token.current {
    border-color: var(--color-gold);
    box-shadow: 0 0 20px rgba(243, 156, 18, 0.5);
}

.mission-token .team-size {
    font-size: 24px;
}

.mission-token .fails-needed {
    font-size: 12px;
    opacity: 0.8;
}

.vote-track {
    text-align: center;
    margin-bottom: 30px;
}

.vote-track h3 {
    color: var(--color-gold);
    margin-bottom: 10px;
}

.vote-tokens {
    display: flex;
    justify-content: center;
    gap: 10px;
}

.vote-token {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    display: flex;
    justify-content: center;
    align-items: center;
    background: rgba(255,255,255,0.1);
    border: 2px solid #555;
    font-weight: bold;
}

.vote-token.active {
    background: var(--color-evil);
    border-color: var(--color-evil);
}

.phase-display {
    text-align: center;
    padding: 30px;
    background: rgba(255,255,255,0.05);
    border-radius: 16px;
    margin-bottom: 30px;
}

.phase-display h2 {
    color: var(--color-gold);
    font-size: 2rem;
    margin-bottom: 10px;
}

.players-circle {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 20px;
    padding: 30px;
}

.circle-player {
    width: 120px;
    text-align: center;
    padding: 15px;
    background: rgba(255,255,255,0.1);
    border-radius: 12px;
    transition: all 0.3s ease;
}

.circle-player.king {
    border: 3px solid var(--color-gold);
    box-shadow: 0 0 15px rgba(243, 156, 18, 0.3);
}

.circle-player.selected {
    background: var(--color-good);
}

.circle-player .crown {
    font-size: 24px;
}

.circle-player .player-name {
    font-weight: bold;
    margin-top: 5px;
}

.team-display, .vote-results, .mission-results {
    text-align: center;
    padding: 20px;
    background: rgba(255,255,255,0.05);
    border-radius: 12px;
    margin-top: 20px;
}

.selected-team {
    display: flex;
    justify-content: center;
    gap: 15px;
    margin-top: 15px;
}

.team-member {
    background: var(--color-good);
    padding: 10px 20px;
    border-radius: 8px;
    font-weight: bold;
}

.vote-columns {
    display: flex;
    gap: 40px;
    justify-content: center;
    margin-top: 15px;
}

.approve-column h4 {
    color: var(--color-good);
    margin-bottom: 10px;
}

.reject-column h4 {
    color: var(--color-evil);
    margin-bottom: 10px;
}

.mission-cards {
    display: flex;
    justify-content: center;
    gap: 20px;
    margin-top: 15px;
}

.mission-card {
    width: 80px;
    height: 120px;
    border-radius: 12px;
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: 36px;
    animation: flipIn 0.5s ease;
}

.mission-card.success-card {
    background: var(--color-good);
}

.mission-card.fail-card {
    background: var(--color-evil);
}

@keyframes flipIn {
    from { transform: rotateY(90deg); }
    to { transform: rotateY(0); }
}

/* Game Over */
.game-over-content {
    text-align: center;
    padding: 60px;
}

.game-over-content h2 {
    font-size: 3rem;
    margin-bottom: 40px;
}

.role-reveal {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 20px;
    margin-bottom: 40px;
}

.reveal-card {
    padding: 20px;
    border-radius: 12px;
    text-align: center;
    min-width: 150px;
}

.reveal-card.good {
    background: var(--color-good);
}

.reveal-card.evil {
    background: var(--color-evil);
}

.reveal-card .reveal-name {
    font-weight: bold;
    font-size: 1.2rem;
}

.reveal-card .reveal-role {
    opacity: 0.9;
    margin-top: 5px;
}

/* ========== Player Page ========== */
.player-container {
    min-height: 100vh;
    padding: 20px;
    max-width: 500px;
    margin: 0 auto;
}

.waiting-content {
    text-align: center;
    padding-top: 100px;
}

.waiting-content h2 {
    color: var(--color-gold);
    font-size: 2rem;
    margin-bottom: 20px;
}

.player-name {
    font-size: 1.2rem;
    margin-bottom: 40px;
}

.player-name span {
    color: var(--color-gold);
    font-weight: bold;
}

.loader {
    width: 50px;
    height: 50px;
    border: 4px solid rgba(255,255,255,0.1);
    border-top-color: var(--color-gold);
    border-radius: 50%;
    margin: 30px auto;
    animation: spin 1s linear infinite;
}

@keyframes spin {
    to { transform: rotate(360deg); }
}

/* Role Card */
#roleScreen {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    padding: 20px;
}

.role-card {
    width: 280px;
    height: 400px;
    perspective: 1000px;
    cursor: pointer;
    margin-bottom: 30px;
}

.role-card-inner {
    position: relative;
    width: 100%;
    height: 100%;
    transition: transform 0.6s;
    transform-style: preserve-3d;
}

.role-card.flipped .role-card-inner {
    transform: rotateY(180deg);
}

.role-card-front, .role-card-back {
    position: absolute;
    width: 100%;
    height: 100%;
    backface-visibility: hidden;
    border-radius: 20px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 20px;
}

.role-card-front {
    background: linear-gradient(135deg, #333, #1a1a1a);
    border: 3px solid var(--color-gold);
}

.role-card-back {
    transform: rotateY(180deg);
    text-align: center;
}

.role-card-back.good {
    background: linear-gradient(135deg, var(--color-good), #2980b9);
}

.role-card-back.evil {
    background: linear-gradient(135deg, var(--color-evil), #c0392b);
}

.role-icon {
    font-size: 80px;
    margin: 20px 0;
}

#roleName {
    font-size: 1.8rem;
}

.role-team {
    font-size: 1.2rem;
    opacity: 0.9;
    margin-bottom: 20px;
}

.role-info {
    font-size: 0.9rem;
    line-height: 1.5;
}

/* Game Panels */
.status-bar {
    display: flex;
    justify-content: space-between;
    padding: 15px;
    background: rgba(0,0,0,0.3);
    border-radius: 12px;
    margin-bottom: 20px;
}

.status-role {
    font-weight: bold;
}

.status-role.good {
    color: var(--color-good);
}

.status-role.evil {
    color: var(--color-evil);
}

.game-panel {
    background: rgba(255,255,255,0.05);
    border-radius: 16px;
    padding: 25px;
    text-align: center;
}

.game-panel h3 {
    color: var(--color-gold);
    margin-bottom: 15px;
}

.player-select-list {
    display: flex;
    flex-direction: column;
    gap: 10px;
    margin: 20px 0;
}

.player-select-item {
    padding: 15px;
    background: rgba(255,255,255,0.1);
    border-radius: 8px;
    cursor: pointer;
    transition: all 0.2s ease;
}

.player-select-item:hover {
    background: rgba(255,255,255,0.2);
}

.player-select-item.selected {
    background: var(--color-good);
}

.proposed-team {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 10px;
    margin: 20px 0;
}

.proposed-team .team-member {
    padding: 10px 15px;
}

.vote-buttons, .mission-buttons {
    display: flex;
    gap: 15px;
    margin-top: 20px;
}

.mission-buttons {
    flex-direction: column;
}

/* Responsive */
@media (max-width: 900px) {
    .lobby-layout {
        grid-template-columns: 1fr;
    }
    
    .logo h1 {
        font-size: 2.5rem;
    }
    
    .mission-token {
        width: 70px;
        height: 70px;
    }
    
    .mission-token .team-size {
        font-size: 18px;
    }
}

@media (max-width: 600px) {
    .game-header {
        flex-direction: column;
        gap: 10px;
    }
    
    .game-header h1 {
        font-size: 1.5rem;
    }
}
